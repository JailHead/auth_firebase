<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- ✅ SIMPLIFICADO: Título básico -->
    <TextView
        android:id="@+id/txtTitulo2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Lista de Contactos"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#273F4F"
        android:gravity="center" />

    <!-- ✅ SIMPLIFICADO: Contador -->
    <TextView
        android:id="@+id/txtContador"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Cargando contactos..."
        android:textSize="16sp"
        android:textColor="#666666"
        android:gravity="center" />

    <!-- ✅ SIMPLIFICADO: ScrollView que SÍ funciona -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="16dp"
        android:background="#F0F0F0"
        android:padding="8dp">

        <!-- ✅ COMPONENTE PRINCIPAL: TextView para contactos -->
        <TextView
            android:id="@+id/txtDetalle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#000000"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:text="Iniciando consulta de contactos..."
            android:lineSpacingExtra="2dp" />

    </ScrollView>

    <!-- ✅ SIMPLIFICADO: Botones básicos -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnActualizar"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Actualizar"
            android:backgroundTint="#4CAF50" />

        <Button
            android:id="@+id/btnRegresar"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Regresar"
            android:backgroundTint="#273F4F" />

    </LinearLayout>

</LinearLayout>